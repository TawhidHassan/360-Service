import 'dart:ui';

class DirectionModel {
  String? address;
  double? lat;
  double? long;
  Color? color;
  //Other fields if needed....
  DirectionModel({this.address, this.color});
//initialise other fields so on....
}